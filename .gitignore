# macOS
.DS_Store

# Xcode user-specific state (the .xcodeproj itself IS tracked — it is the single source of truth)
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Build artifacts
DerivedData/
build/

# Swift Package Manager
.build/
.swiftpm/
Package.resolved
